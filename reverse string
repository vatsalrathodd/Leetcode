class Solution {
public:
    void reverseString(vector<char>& s) {
        
        // 1st Method
        reverse(s.begin(),s.end());
        
        // 2nd Method
        // stack<char> myStack;

        // for(char c : s)
        // {
        //     myStack.push(c);
        // }

        // for(int i=0; i<s.size(); i++)
        // {
        //     s[i] = myStack.top();
        //     myStack.pop();
        // }

    }
};
